# This file is intentionally minimal.
# All production env vars are set directly in Vercel dashboard/CLI.
# DO NOT set AUTH_SECRET, DATABASE_URL, or NEXTAUTH_URL here —
# empty values here OVERRIDE Vercel env vars and break Auth.js.
